import {getBaseUrl} from "@/utils";

export const UserUrls = {
  user: `${getBaseUrl()}/user`,
  login: `${getBaseUrl()}/login`,
  logout: `${getBaseUrl()}/logout`,
  addUserRole: `${getBaseUrl()}/user/role`,
}

